# Optional in development because Vite proxies Socket.IO to localhost:5000.
VITE_SOCKET_URL=

# Required to enable the Run button's compiler request.
VITE_COMPILER_API_URL=https://your-compiler-service.example

# Comma-separated origins accepted by the production Socket.IO server.
SOCKET_ALLOWED_ORIGINS=https://your-app.example
